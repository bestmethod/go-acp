* feature: allow copy of dirA->dirB to copy dirA/dirB if dirB/, otherwise dirA/* to dirB (depending on whether there is `/` at the end)
* feature: double-check written file size / md5 sums options for the paranoid
